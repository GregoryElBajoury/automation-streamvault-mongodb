# Identifiants administrateur pour la base MongoDB
MONGO_INITDB_ROOT_USERNAME=
MONGO_INITDB_ROOT_PASSWORD=

# Port d écoute pour la base de données MongoDB
MONGO_PORT=27017

# Port pour linterface de visualisation WebVOWL (optionnel)
WEBVOWL_PORT=8080
